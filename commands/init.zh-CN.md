# /vscode-ui-test-init

为 VS Code 扩展仓库注入 Workbench UI 测试脚手架。

步骤：
1. 在仓库根目录执行 `npx create-vedats@latest`。
2. 确认 `tests/ui/**`、`.github/workflows/ui-tests.yml` 和 scripts 已生成。
3. 运行 `npm run test:ui` 并返回 artifacts 路径。

## English Version

- [init.md](init.md)
